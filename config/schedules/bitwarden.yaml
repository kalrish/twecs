---

parameters:
   FunctionArn: !stack_output wise.yaml::FunctionArn
   ScheduleExpression: >-
      cron(0 7 15 * ? *)
   State: ENABLED

sceptre_user_data:
   wise:
      amount: 0.84
      amount_side: target
      reference: Bitwarden
      source_currency: EUR
      target_currency: USD

stack_tags:
   Purpose: twecs

template_path: sceptre/schedule.py
